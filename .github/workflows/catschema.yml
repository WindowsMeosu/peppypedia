name: catschema

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2019

    steps:
      - uses: actions/checkout@v1

      - name: curl
        run: curl https://milotilo.ddns.net/peppypediadata/page_schema.xsd

      - name: winget
        run: winget https://milotilo.ddns.net/peppypediadata/page_schema.xsd
         
